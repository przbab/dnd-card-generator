name: Lint

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

permissions:
  checks: write
  contents: write

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Install Node.js dependencies
      run: npm ci
  eslint:
    needs: setup
    - name: Run eslint
      npm run eslint
  stylelint:
    needs: setup
    - name: Run stylelint
      npm run stylelint

